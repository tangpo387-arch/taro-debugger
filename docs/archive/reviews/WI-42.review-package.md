---
wi: WI-42
title: "Command Serialization: Frame Switch Cancel-and-Replace"
author: Lead_Engineer
status: ready-for-review
skills-required: [DEV:ANG, DEV:STATE, DEV:DAP]
---

# Review Package: WI-42

## 1. Acceptance Criteria

> Copied verbatim from `node scripts/manage-wi.js show WI-42`.

- [x] Refactor `DebuggerComponent.onFrameClick` to use a `frameSelected$` Subject and `switchMap`
- [x] Refactor `DapVariablesService` to guard against stale `fetchScopes` promises
- [x] No Session Layer changes required
- [x] [Test] rapid frame clicks result in only the last frame's source, disassembly, and scopes being rendered
- [x] [Doc] `docs/frame-switch-serialization-spec.md`

## 2. Diff Summary

| File | Changed Lines | Nature of Change |
| :--- | :--- | :--- |
| `projects/taro-debugger-frontend/src/app/debugger.component.ts` | L5-6, L110-111, L249-297, L593-649 | Implemented `frameSelected$` Subject + `switchMap` orchestration. Moved async logic out of `onFrameClick`. |
| `projects/ui-inspection/src/lib/dap-variables.service.ts` | L47, L50-69 | Added `lastRequestedFrameId` anti-race guard to `fetchScopes`. |
| `projects/ui-inspection/src/lib/dap-variables.service.spec.ts` | L142-167 | Added unit test for `fetchScopes` anti-race guard. |

## 3. Edge Cases & Design Decisions

- **Decision**: Kept synchronous state updates (active line/frame ID) inside `onFrameClick` to provide immediate UI feedback, while offloading network-heavy tasks (source, disassembly, scopes) to the `switchMap` stream.
- **Decision**: Maintained the `async` signature for `DapVariablesService.fetchScopes` to preserve compatibility with existing tests and callers, but implemented the guard internally using a timestamp/ID check.
- **🔍 Inspect**: The `initFrameSelectionStream` in `DebuggerComponent` — verify that `forkJoin` correctly handles cases where some tasks (like source loading) are skipped (it should, as they are conditionally added to the array).

## 4. Tests Added

| File | Suite | Test Case |
| :--- | :--- | :--- |
| `projects/taro-debugger-frontend/src/app/wi-42-frame-switch-serialization.spec.ts` | `WI-42: Frame Switch Serialization` | should only render the last frame data when multiple frames are clicked rapidly |
| `projects/ui-inspection/src/lib/dap-variables.service.spec.ts` | `DapVariablesService -> fetchScopes` | should ignore stale responses (anti-race guard) |

## 5. Spec-Plan Updates

| Spec-Plan File | Section Added |
| :--- | :--- |
| N/A | No new spec-plan required for this S-sized refactor; behavior covered by existing `wi-42-frame-switch-spec.md`. |

## 6. Self-Verification

### UI Inspection Tests (including anti-race guard)

```text
 ✓ |ui-inspection| projects/ui-inspection/src/lib/dap-variables.service.spec.ts (27 tests) 123ms
 Test Files  1 passed (1)
      Tests  27 passed (27)
```

### Frontend Serialization Test

```text
 ✓ |taro-debugger-frontend| projects/taro-debugger-frontend/src/app/wi-42-frame-switch-serialization.spec.ts (1 test) 187ms
   ✓ WI-42: Frame Switch Serialization (1)
     ✓ should only render the last frame data when multiple frames are clicked rapidly 186ms
```
