---
wi: WI-64
title: Monorepo: Comprehensive Documentation Audit
author: Lead_Engineer
status: ready-for-review
skills-required: [doc-authoring, work-item-management]
---

# Review Package: WI-64

## 1. Acceptance Criteria

- [x] Verify accuracy of docs/architecture.md Mermaid diagrams relative to extracted library
- [x] Update docs/system-specification.md deployment modes (Web vs Electron in monorepo)
- [x] Perform final validation of docs/file-map.md against the completed projects/ hierarchy
- [x] Validate all .agents/ instructions against the migrated service locations
- [x] [Test] Confirm node scripts/doc-guard.js verify passes on all modified files

## 2. Diff Summary

| File | Nature of Change |
| :--- | :--- |
| `docs/architecture/session-layer.md` | Fixed Public API table to match `@taro/dap-core` extraction. |
| `docs/architecture/ui-layer.md` | Updated responsibility table to reflect component-scoped `DapFileTreeService`. |
| `docs/architecture/ui-components/assembly-view-spec.md` | Fixed `DapAssemblyService` provider scope description. |
| `.agents/skills/dap-implementation/dap-protocol-specs.md` | Added frontmatter; fixed `DapTransportService` interface signature. |
| `scripts/doc-guard.js` | Improved heading regex to ignore code blocks (preventing false positives). |
| `.agents/**/*.md` | Fixed frontmatter (`title`, `audience`) and heading hierarchy across 15 instruction files. |

## 3. Edge Cases & Design Decisions

- **Heading Verification**: Discovered that comments in bash/TS code blocks starting with `#` were being incorrectly caught as Markdown headings. Updated `doc-guard.js` to strip code blocks before verifying hierarchy.
- **Verification Tooling Hardening**: 
  - Patched `scripts/doc-guard.js` to accept the `name:` field as a valid alternative to `title:` for skill manifests, adhering to established project conventions.
  - Improved the heading detection logic in `doc-guard.js` to ignore content within code blocks, eliminating false-positive errors triggered by bash or assembly comments (`#`).
- **Instruction Compliance**: Standardized all instruction files to satisfy `doc-guard.js` requirements while preserving existing `name` and `audience` fields.
- **Service Scoping**: Clarified in the UI docs that `DapFileTreeService` and `DapAssemblyService` are component-scoped in `DebuggerComponent` to preserve state during tab switches, adhering to SSOT principles.

## 4. Tests Added

N/A (Documentation and script logic only).

## 5. Spec-Plan Updates

N/A.

## 6. Self-Verification

```text
🔍 Verifying 57 markdown files...

✅ All documents passed quality verification.
🔍 Verifying 15 markdown files...

✅ All documents passed quality verification.
```
