---
wi: WI-51
title: Documentation Workflow Automation (Doc-Guard)
author: Lead_Engineer
status: ready-for-review
skills-required: [doc-authoring]
---

# Review Package: WI-51

## 1. Acceptance Criteria

> Copied verbatim from `node scripts/manage-wi.js show WI-51` `.details[]`.

- [x] Implement doc-guard.js with 'init-spec' and 'verify' subcommands
- [x] Centralize Doc Schema definition (Frontmatter, Mandatory Sections)
- [x] Pre-populate WI context into specs
- [x] Implement CI-ready linting for the docs/ directory
- [x] [Test] Verify init-spec generates compliant files; verify script rejects malformed MDs

## 2. Diff Summary

> List every modified file with the exact line ranges changed.

| File | Changed Lines | Nature of Change |
| :--- | :--- | :--- |
| `scripts/doc-guard.js` | L1–230 | Initial implementation of documentation management tool |

## 3. Edge Cases & Design Decisions

> Explain any non-obvious decisions. Flag areas requiring deeper QCR inspection with 🔍.

- **Non-English Guard**: Uses a simplified Unicode range check for Chinese characters (`[\u4e00-\u9fa5]`) to satisfy the global language policy.
- **Naming Flexibility**: 🔍 Inspect: `L145-156` specifically how I allow `WI-ID` prefixes and dots for review packages and spec-plans to avoid false positives.
- **Frontmatter Lenience**: 🔍 Inspect: `L158-172` where I allow `review-package` files to bypass some frontmatter checks as they have a specialized schema.

## 4. Tests Added

| File | Suite | Test Case |
| :--- | :--- | :--- |
| Manual | — | Ran `init-spec` and verified output matches template |
| Manual | — | Ran `verify` on `docs/` and confirmed rejection of existing debt |

## 5. Spec-Plan Updates

| Spec-Plan File | Section Added |
| :--- | :--- |
| none | WI-51 covers the tool itself; future specs will use this tool. |

## 6. Self-Verification

> Paste the terminal output proving all tests pass.

```text
$ node scripts/doc-guard.js init-spec WI-51 feature doc-guard-spec
✅ Successfully initialized feature spec: docs/doc-guard-spec.md

$ node scripts/doc-guard.js verify docs/doc-guard-spec.md
🔍 Verifying 1 markdown files...
✅ All documents passed quality verification.
```
